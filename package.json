{
  "name": "encrypted-file-transfer-secure-storage",
  "version": "1.0.0",
  "private": true,
  "description": "Client-side encrypted, chunked file transfer with integrity checks and resumable uploads.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "upload": "node client.js upload",
    "download": "node client.js download"
  }
}
